
Versions:

1. 3 replicas, 200 epochs, 13 hidden nodes with sigmoid, standard BSA1 HERMES data, 12 + 6 = 18 total

2. 50 replicas, 200 epochs, 13 hidden nodes with sigmoid, standard BSA1 HERMES data, 12 + 6 = 18 total

Comments:

1. `v1_1` plots show the $N_{\text{replica}} = 3$ replica *average* fit (orange line) of the first sine-harmonic of the BSA. The CI (confidence intervals) are not shown because I did not set up that feature yet.

2. `v1_2` plots show the $N_{\text{replica}} = 50$ replica *average* fit with a solid, red line. The band defined by the standard deviation of the replica averaging procedure (`np.std()`) is shown with the opaque, red band with forward-slash hatch marks. It was generated by adding and subtracting the standard deviation from the mean value at every point along the $x$-axis.

